#!/usr/bin/env grrovy

pipeline {

node{
}
stage{
        stages(Build){
		
		echo "welcome to build evironament"
		
		sh date
		
		}
		stages(Test){
		
		echo "welcome to build evironament"
		
		sh cal
		
		}
		stages(Stage){
		
		echo "welcome to build evironament"
		sh 'cat /etc/passwd'
		}
}

}
